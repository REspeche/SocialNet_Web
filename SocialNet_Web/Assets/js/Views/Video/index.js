mainApp.controller("videoCtrl", videoCtrl);
videoCtrl.$inject = ["$rootScope", "$scope", "mainService"];

function videoCtrl($rootScope, $scope, mainService) {
    $scope.loadInit = function () {

    };
}